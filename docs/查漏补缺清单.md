# 查漏补缺清单（对照廖雪峰《Python教程》）

> 依据：https://liaoxuefeng.com/books/python/introduction/index.html  
> 目标：标记现有仓库覆盖的章节与缺口，便于后续补全。

## 已覆盖（示例章节）
- 语法基础、条件/循环、容器、函数：`01.Python语言基础/01-06`
- 异常处理与文件：`01.Python语言基础/08_异常与文件基础`
- 面向对象入门与特殊方法：`01.Python语言基础/09_面向对象入门`
- 迭代器/生成器、lambda/装饰器：`01.Python语言基础/10`、`11`
- 模块与包：`01.Python语言基础/07_模块与包基础`
- Pythonic 写法：`01.Python语言基础/14_开发技巧_写法优化`
- 正则、网络、多线程/多进程、异步：`01.Python语言基础/15_进阶专题`
- Web/FastAPI 实战：`03.项目实战/03_TODO_Web_API_FastAPI`、`integration_gateway_service` 等

## 缺口清单（按优先级）
1. **函数式编程细节** ✅ 已补：`demo_hof_partial.py`（map/filter/reduce + partial），并在文档与前端导航同步。  
   - 位置：`01.Python语言基础/11_lambda表达式与装饰器/`
2. **面向对象高级** ✅ 已补：`advanced_oop.md` + `demo_oop_advanced.py`，涵盖 @property/__slots__/可迭代/元类；前端导航已同步。  
3. **调试与测试专题** ✅ 已补：`08_调试与性能分析/08_调试与性能分析.md` + `demo_debug_profiling.py`，涵盖 pdb/logging/timeit/cProfile/pytest；前端导航已同步。  
4. **常用标准库空白** ✅ 已补：`contextlib`/`pairwise`/`batched`/`cached_property`/`hashlib`/`hmac`/`base64` 等，新增 `demo_stdlib_adv.py`；标准库文档已扩展。  
5. **IO 扩展** ✅ 已补：`09_IO扩展/09_IO扩展.md` + `demo_io_ext.py` 覆盖 StringIO/BytesIO/struct/gzip/zipfile，并在前端导航同步。
6. **并发进阶** ✅ 已补：`高级并发技巧.md` + `demo_concurrency_adv.py`（threading.local、进程池）；`asyncio进阶.md` + `demo_asyncio_adv.py`（gather/超时/取消）。
7. **网络/HTTP 客户端** ✅ 已补：`网络客户端进阶.md` + `demo_network_clients.py`（urllib/requests/httpx Mock、超时与重试）；前端导航同步。
8. **Mock & 离线联调** ✅ 已补：`tests/fixtures/` 提供 fake MySQL/Redis/MinIO；`.env.example` 占位；portal 增 Mock 入口；frontend/README mock 段落可直接引用。
8. **数据库与持久化**：教程涵盖 SQLite/MySQL 接口；仓库仅示范 SQLAlchemy。  
   - 补充位置建议：`03.项目实战/03_TODO_Web_API_FastAPI` 文档追加“原生 DB-API 示例链接”。
9. **部署与虚拟环境**：虚拟环境已覆盖，但缺少 `pipx/uv`、`pyenv` 对比。  
   - 补充位置建议：`docs/快速开始与依赖.md` 增加“可选工具”段落。

## 近期可执行事项（建议顺序）
1. 新增高阶函数示例脚本并在 README 的学习路径中标注。
2. 在 `15_进阶专题` 内补一页“调试与性能分析”并附 pytest/pdb 使用示例。
3. 在标准库专题补充 `contextlib` + `itertools` 小节及可运行 demo。
4. 增加 `httpx`/`aiohttp` 示例，配合网关服务形成对照。

> 更新本清单时，请保持“缺口项描述 + 建议落地位置 + 可执行动作”三要素，避免再次产生分散文档。
