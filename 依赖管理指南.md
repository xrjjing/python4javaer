# 依赖管理指南

> ⚠️ 本文已合并并精简为 `docs/快速开始与依赖.md`，建议优先阅读新文档。以下内容保留作历史参考。

本文档详细说明项目中三个依赖管理文件的作用、使用方法和最佳实践。

---

## 📋 目录

1. [文件概述](#文件概述)
2. [requirements.txt - 核心依赖](#requirementstxt---核心依赖)
3. [requirements-dev.txt - 开发依赖](#requirements-devtxt---开发依赖)
4. [pyproject.toml - 项目配置](#pyprojecttoml---项目配置)
5. [使用场景](#使用场景)
6. [常见问题](#常见问题)

---

## 文件概述

### 三个文件的关系

```
pyproject.toml              ← 现代项目配置（元数据 + 依赖 + 工具配置）
    ↓
requirements.txt            ← 核心运行时依赖（生产环境）
    ↓
requirements-dev.txt        ← 开发依赖（包含 requirements.txt + 开发工具）
```

### 对比表

| 文件 | 作用 | 包含内容 | 使用场景 |
|------|------|---------|---------|
| `requirements.txt` | 运行时依赖 | FastAPI、pandas等核心库 | 生产部署、Docker |
| `requirements-dev.txt` | 开发依赖 | 核心依赖 + pytest、black等 | 本地开发、CI/CD |
| `pyproject.toml` | 项目配置 | 元数据 + 依赖 + 工具配置 | 现代Python标准 |

---

## requirements.txt - 核心依赖

### 📄 文件内容

```txt
# Python学习项目 - 核心依赖
# 用于项目实战部分的第三方库

# Web框架（项目03）
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# 数据库ORM（项目03）
sqlalchemy>=2.0.0

# HTTP请求（项目05, 08）
requests>=2.31.0

# HTML解析/爬虫（项目05）
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 数据分析（项目04）
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.0  # Excel文件支持
```

### 🎯 作用

列出项目**运行必需**的第三方库，这些库是：
- ✅ 运行demo代码需要的
- ✅ 运行实战项目需要的
- ✅ 生产部署需要的

### 📦 安装方法

```bash
# 基础安装
pip install -r requirements.txt

# 查看安装了哪些包
pip list

# 验证安装
python -c "import fastapi; print(fastapi.__version__)"
```

### ⚙️ 版本说明

```txt
# 版本指定方式
fastapi==0.104.0        # 精确版本（不推荐，太严格）
fastapi>=0.104.0        # 最低版本（推荐）✅
fastapi>=0.104.0,<1.0.0 # 版本范围
fastapi~=0.104.0        # 兼容版本（0.104.x）
```

**推荐使用 `>=` 方式**，允许安装更新的兼容版本。

### 🚀 使用场景

#### 1. Docker部署
```dockerfile
FROM python:3.11-slim

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "app.py"]
```

#### 2. 云服务器部署
```bash
# 在服务器上
git clone <your-repo>
cd python-learning
pip install -r requirements.txt
```

#### 3. 最小化安装
```bash
# 只想运行某个特定项目
pip install fastapi uvicorn sqlalchemy  # 手动选择
```

---

## requirements-dev.txt - 开发依赖

### 📄 文件内容

```txt
# Python学习项目 - 开发依赖
# 用于开发、测试和代码质量检查

# 首先安装核心依赖
-r requirements.txt

# 测试框架
pytest>=7.4.0
pytest-cov>=4.1.0  # 测试覆盖率
pytest-asyncio>=0.21.0  # 异步测试支持

# 代码质量
black>=23.10.0  # 代码格式化
flake8>=6.1.0  # 代码风格检查
pylint>=3.0.0  # 代码质量检查
isort>=5.12.0  # import排序

# 类型检查
mypy>=1.6.0
types-requests>=2.31.0

# 开发工具
ipython>=8.17.0  # 交互式Python
jupyter>=1.0.0  # Jupyter notebook
jupyterlab>=4.0.0  # JupyterLab
```

### 🎯 作用

包含开发时需要的所有工具：
- ✅ 测试框架（pytest）
- ✅ 代码格式化（black）
- ✅ 代码检查（flake8、mypy）
- ✅ 交互式环境（IPython、Jupyter）

### 📦 安装方法

```bash
# 安装所有开发依赖（推荐）
pip install -r requirements-dev.txt

# 这会自动安装：
# 1. requirements.txt 中的所有包
# 2. requirements-dev.txt 中的额外工具
```

### 🔧 开发工具使用

#### 1. 代码格式化
```bash
# 格式化所有Python文件
black .

# 格式化特定文件
black 01.Python语言基础/08_异常与文件基础/
```

#### 2. 代码检查
```bash
# 检查代码风格
flake8 .

# 类型检查
mypy .
```

#### 3. 运行测试
```bash
# 运行所有测试
pytest

# 运行特定测试
pytest 03.项目实战/03_TODO_Web_API_FastAPI/

# 查看测试覆盖率
pytest --cov=. --cov-report=html
```

#### 4. 交互式开发
```bash
# 启动 IPython
ipython

# 启动 Jupyter Lab
jupyter lab
```

### 🚀 使用场景

#### 1. 本地开发
```bash
# 首次克隆项目
git clone <repo>
cd python-learning
pip install -r requirements-dev.txt
```

#### 2. CI/CD 测试
```yaml
# .github/workflows/test.yml
- name: Install dependencies
  run: pip install -r requirements-dev.txt

- name: Run tests
  run: pytest

- name: Check code style
  run: black --check .
```

#### 3. 代码质量检查
```bash
# 提交前检查
black .
flake8 .
mypy .
pytest
```

---

## pyproject.toml - 项目配置

### 📄 文件结构

```toml
[build-system]              # 构建系统配置

[project]                   # 项目元数据
name = "python-learning"
version = "1.0.0"
dependencies = [...]        # 核心依赖

[project.optional-dependencies]
dev = [...]                 # 开发依赖

[tool.black]                # Black配置
line-length = 100

[tool.pytest.ini_options]   # pytest配置
testpaths = ["tests"]

[tool.mypy]                 # mypy配置
python_version = "3.10"
```

### 🎯 作用

**一站式项目配置文件**，包含：

1. **项目元数据**
   - 名称、版本、作者
   - 许可证、关键字
   - 项目链接

2. **依赖管理**
   - 核心依赖（替代 requirements.txt）
   - 可选依赖（开发、测试等）

3. **工具配置**
   - Black（代码格式化）
   - pytest（测试）
   - mypy（类型检查）
   - isort（import排序）
   - coverage（覆盖率）

### 📦 使用方法

#### 1. 安装项目
```bash
# 安装项目及核心依赖
pip install -e .

# 安装项目及开发依赖
pip install -e ".[dev]"
```

#### 2. 工具配置自动生效

所有工具会自动读取 `pyproject.toml` 配置：

```bash
# Black 自动使用 [tool.black] 配置
black .

# pytest 自动使用 [tool.pytest.ini_options] 配置
pytest

# mypy 自动使用 [tool.mypy] 配置
mypy .
```

### 🔧 关键配置说明

#### Black配置
```toml
[tool.black]
line-length = 100           # 每行最多100字符
target-version = ['py310']  # 目标Python版本
```

#### pytest配置
```toml
[tool.pytest.ini_options]
testpaths = ["01.Python语言基础", "03.项目实战"]
addopts = ["-ra", "--cov=."]  # 默认参数
```

#### mypy配置
```toml
[tool.mypy]
python_version = "3.10"
warn_return_any = true      # 警告返回Any类型
check_untyped_defs = true   # 检查无类型注解的函数
```

### 🚀 使用场景

#### 1. 现代Python项目标准
```bash
# 替代传统的 setup.py
pip install -e .  # 开发模式安装
```

#### 2. 发布到PyPI
```bash
# 构建包
python -m build

# 发布
python -m twine upload dist/*
```

#### 3. 工具配置集中管理
```bash
# 所有工具配置在一个文件中
# 不需要：
# - .flake8
# - pytest.ini
# - mypy.ini
# 等多个配置文件
```

---

## 使用场景

### 🎓 场景1：学习者初次使用

```bash
# 步骤1：克隆项目
git clone <repo>
cd python-learning

# 步骤2：创建虚拟环境
python -m venv venv
source venv/bin/activate

# 步骤3：安装开发依赖（推荐）
pip install -r requirements-dev.txt

# 步骤4：开始学习
python 01.Python语言基础/01_快速上手与基本语法/demo_hello.py
```

### 🏭 场景2：生产部署

```bash
# 只安装运行时依赖
pip install -r requirements.txt

# 运行应用
python app.py
```

### 🔬 场景3：开发新功能

```bash
# 1. 安装完整开发环境
pip install -r requirements-dev.txt

# 2. 开发代码
vim new_feature.py

# 3. 格式化代码
black new_feature.py

# 4. 运行测试
pytest tests/test_new_feature.py

# 5. 类型检查
mypy new_feature.py
```

### 🐳 场景4：Docker容器化

```dockerfile
# Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 只复制 requirements.txt（不要dev）
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

### 🤖 场景5：CI/CD

```yaml
# .github/workflows/test.yml
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest

      - name: Check code style
        run: |
          black --check .
          flake8 .
```

---

## 常见问题

### Q1: 我应该用哪个文件？

**日常开发**：
```bash
pip install -r requirements-dev.txt
```

**生产部署**：
```bash
pip install -r requirements.txt
```

**现代方式**：
```bash
pip install -e ".[dev]"  # 使用 pyproject.toml
```

### Q2: 三个文件的依赖会冲突吗？

不会。因为：
- `requirements-dev.txt` 包含 `requirements.txt`（通过 `-r requirements.txt`）
- `pyproject.toml` 中 `[project.optional-dependencies]` 是额外的

### Q3: 如何更新依赖？

```bash
# 查看过期的包
pip list --outdated

# 更新单个包
pip install --upgrade fastapi

# 更新所有包（慎重！可能导致不兼容）
pip install --upgrade -r requirements.txt

# 推荐：更新后测试
pip install --upgrade pytest
pytest  # 确保测试通过
```

### Q4: 如何锁定依赖版本？

```bash
# 生成精确版本的依赖文件
pip freeze > requirements.lock

# 在生产环境使用锁定版本
pip install -r requirements.lock
```

### Q5: 为什么有些包没在 requirements.txt 中？

**标准库不需要**（内置）：
- `os`, `sys`, `json`, `csv`, `datetime` 等

**只有第三方库需要**：
- `fastapi`, `requests`, `pandas` 等

### Q6: 如何添加新依赖？

#### 方法1：手动添加（推荐）
```bash
# 1. 编辑 requirements.txt
echo "新包名>=版本" >> requirements.txt

# 2. 安装
pip install -r requirements.txt

# 3. 同步到 pyproject.toml
# 编辑 pyproject.toml 的 dependencies 列表
```

#### 方法2：先安装再记录
```bash
# 1. 安装包
pip install 新包名

# 2. 记录到文件
pip freeze | grep 新包名 >> requirements.txt
```

### Q7: 虚拟环境和依赖文件的关系？

```bash
# 虚拟环境：隔离的Python环境
python -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 在虚拟环境中安装依赖
pip install -r requirements-dev.txt

# 所有包安装在虚拟环境中，不影响系统Python
```

### Q8: 如何查看已安装的包？

```bash
# 查看所有包
pip list

# 查看特定包
pip show fastapi

# 生成当前环境的依赖列表
pip freeze
```

---

## 最佳实践

### ✅ 推荐做法

1. **始终使用虚拟环境**
   ```bash
   python -m venv venv
   source venv/bin/activate
   ```

2. **开发时安装dev依赖**
   ```bash
   pip install -r requirements-dev.txt
   ```

3. **提交代码前检查**
   ```bash
   black .
   flake8 .
   pytest
   ```

4. **定期更新依赖**
   ```bash
   pip list --outdated
   pip install --upgrade <package>
   ```

5. **生产环境锁定版本**
   ```bash
   pip freeze > requirements.lock
   ```

### ❌ 避免的做法

1. ❌ 不使用虚拟环境（污染系统Python）
2. ❌ 直接修改 site-packages
3. ❌ 混用 pip 和 conda
4. ❌ 提交 `venv/` 或 `__pycache__/` 到Git
5. ❌ 在生产环境安装开发依赖

---

## 与Java的对比

| Python | Java (Maven) | 说明 |
|--------|-------------|------|
| `requirements.txt` | `<dependencies>` | 依赖列表 |
| `requirements-dev.txt` | `<scope>test</scope>` | 测试依赖 |
| `pyproject.toml` | `pom.xml` | 项目配置 |
| `pip install` | `mvn install` | 安装依赖 |
| `venv/` | `.m2/` | 依赖缓存 |
| `pip freeze` | `mvn dependency:tree` | 查看依赖 |

---

## 快速参考

```bash
# 安装
pip install -r requirements.txt          # 核心依赖
pip install -r requirements-dev.txt      # 开发依赖
pip install -e ".[dev]"                  # 使用pyproject.toml

# 更新
pip install --upgrade -r requirements.txt

# 查看
pip list                                  # 所有包
pip show <package>                        # 包详情
pip freeze                                # 精确版本

# 工具
black .                                   # 格式化
flake8 .                                  # 检查风格
mypy .                                    # 类型检查
pytest                                    # 运行测试
```

---

## 总结

三个文件各司其职：

- **requirements.txt** → 运行时必需（生产）
- **requirements-dev.txt** → 开发工具（开发）
- **pyproject.toml** → 现代标准（配置中心）

**推荐使用方式**：
- 开发：`pip install -r requirements-dev.txt`
- 部署：`pip install -r requirements.txt`
- 现代：`pip install -e ".[dev]"`

选择最适合你的方式即可！✨
