# 02 路由与依赖注入进阶（FastAPI vs Spring Boot 视角）

> 面向对象：从 Java / Spring Boot 迁移到 Python / FastAPI 的同学。
> 目标:学会把中小型单体接口拆分成可维护的路由模块，熟练使用 `Depends` 进行多层依赖注入、测试替换与生命周期管理。

---

## 0. 快速对照：Spring Boot DI vs FastAPI Depends

| 维度 | Spring Boot | FastAPI |
| --- | --- | --- |
| 注入语法 | `@Autowired` / 构造器注入 | `Depends(callable)` |
| Bean 定义 | `@Bean` / `@Service` / `@Component` | 普通函数或类实例,返回值即依赖 |
| 作用域 | singleton / request / session 等 | 默认"每次调用"创建；可在依赖内部缓存 |
| 生命周期 | `@PostConstruct` / `@PreDestroy` | `lifespan`（startup/shutdown）或依赖中的 `yield` |
| 配置切换 | `@Profile` / 条件装配 | `dependency_overrides`（测试/AB）或注入工厂函数 |
| 测试替身 | `@MockBean` | `app.dependency_overrides[...] = fake_impl` |

**记忆句**：`Depends` 就像"轻量函数级 DI + 请求作用域 Bean"，没有复杂容器，靠 Python 函数组合即可。

---

## 1. 路由拆分与 APIRouter 设计模式

### 1.1 何时拆分
- **领域分界**：用户、商品、订单各自独立路由文件。
- **团队协作**：不同子团队维护不同 router。
- **中间件差异**：需要局部 CORS/鉴权/速率限制时，用 router 级装饰。
- **版本演进**：`/v1`、`/v2` 分包，降低互相污染。

### 1.2 典型结构
```
app/
  main.py            # 入口，挂载各 router
  deps.py            # 常用依赖（DB、配置、当前用户）
  routers/
    users.py
    products.py
    health.py
```

### 1.3 "Before → After" 对比

**Before：所有接口挤在 main.py**
```python
# app/main.py
from fastapi import FastAPI, Depends
from sqlalchemy.orm import Session
from .db import get_db

app = FastAPI()

@app.get("/users/{uid}")
def get_user(uid: int, db: Session = Depends(get_db)):
    ...

@app.get("/products/{pid}")
def get_product(pid: int, db: Session = Depends(get_db)):
    ...
```
缺点：文件不断膨胀，路由难以查找，测试也难拆。

**After：按领域拆分 APIRouter**
```python
# app/routers/users.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.deps import get_db

router = APIRouter(prefix="/users", tags=["users"])

@router.get("/{uid}")
def get_user(uid: int, db: Session = Depends(get_db)):
    return {"id": uid, "name": "Alice"}

# app/routers/products.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.deps import get_db

router = APIRouter(prefix="/products", tags=["products"])

@router.get("/{pid}")
def get_product(pid: int, db: Session = Depends(get_db)):
    return {"id": pid, "title": "Keyboard"}

# app/main.py
from fastapi import FastAPI
from app.routers import users, products

app = FastAPI()
app.include_router(users.router)
app.include_router(products.router)
```
效果：按文件名即可定位路由；可为 `users.router` 单独加鉴权或速率限制。

### 1.4 Router 层中间件示例
```python
# 仅给用户接口加简单鉴权
@router.middleware("http")
async def user_auth_mw(request, call_next):
    token = request.headers.get("X-User-Token")
    if token != "demo":
        return JSONResponse(status_code=401, content={"detail": "unauthorized"})
    return await call_next(request)
```

---

## 2. Depends 进阶：多层依赖与依赖链

### 2.1 基本回顾
- 依赖可以是函数、可调用对象或类实例。
- 返回值直接注入到路由参数。
- 异步/同步均可。

### 2.2 多级依赖链

**示例：配置 → 数据库 → 仓储 → 路由**
```python
# app/deps.py
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session
from functools import lru_cache

class Settings(BaseModel):
    db_url: str = "sqlite:///./app.db"

@lru_cache
def get_settings() -> Settings:
    return Settings()

def get_engine(settings: Settings = Depends(get_settings)):
    return create_engine(settings.db_url, connect_args={"check_same_thread": False})

def get_db(engine = Depends(get_engine)) -> Generator[Session, None, None]:
    SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# app/routers/users.py
def get_user_repo(db: Session = Depends(get_db)):
    return UserRepository(db)

@router.get("/{uid}")
def get_user(uid: int, repo = Depends(get_user_repo)):
    return repo.get(uid)
```
**链路解析**：`get_user` → `get_user_repo` → `get_db` → `get_engine` → `get_settings`。
每层可独立替换，测试或 AB 实验时只覆盖某一层即可。

### 2.3 类依赖（带状态）
```python
class CurrentUser:
    def __init__(self, token: str = Header(...)):
        self.token = token
    def __call__(self):
        # 这里可解析 JWT，简化为返回假用户
        return {"id": 1, "name": "Alice"}

@router.get("/me")
def me(user = Depends(CurrentUser())):
    return user
```
类的 `__call__` 使其可作为依赖；可携带初始化参数，类似"带构造参数的 Bean"。

### 2.4 带 `yield` 的依赖（资源管理）
```python
def get_db(engine=Depends(get_engine)):
    SessionLocal = sessionmaker(bind=engine)
    db = SessionLocal()
    try:
        yield db   # ← 路由执行期间可用
    finally:
        db.close() # ← 收尾，等价 @PreDestroy
```
`yield` 前为"获取资源"，之后为"释放资源"，与 Spring `@PreDestroy` 心智相似。

---

## 3. 依赖覆盖（testing / A/B 配置）

### 3.1 测试中的 Fake DB
```python
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.deps import get_db

def override_db():
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    TestingSession = sessionmaker(bind=engine)
    db = TestingSession()
    try:
        yield db
    finally:
        db.close()

app.dependency_overrides[get_db] = override_db
client = TestClient(app)

def test_list_users():
    r = client.get("/users/1")
    assert r.status_code == 200
```
**要点**：覆盖只在当前 `app` 实例生效，结束测试可清理 `dependency_overrides`。

### 3.2 A/B 或多配置切换
```python
def get_repo(
    db: Session = Depends(get_db),
    use_cache: bool = Depends(lambda: True)  # 可被覆盖
):
    if use_cache:
        return CachedUserRepo(db)
    return UserRepo(db)

# 测试切换
app.dependency_overrides[lambda: True] = lambda: False
```
用覆盖做"条件装配"，无须 Profile。

---

## 4. 生命周期（lifespan）与启动/关闭钩子

### 4.1 FastAPI 方式
- **全局**：`FastAPI(lifespan=lifespan_context)`。
- **常见用途**：建立长连接（DB pool、Redis、消息队列）、预热缓存、注册指标钩子。

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI

@asynccontextmanager
async def lifespan(app: FastAPI):
    # startup
    app.state.redis = await create_redis_pool()
    yield
    # shutdown
    await app.state.redis.close()

app = FastAPI(lifespan=lifespan)
```

### 4.2 与依赖的关系
- `lifespan` 里放"全局单例"资源。
- 依赖里再读 `app.state.*`，避免每请求重建。
```python
def get_redis(request: Request):
    return request.app.state.redis
```

### 4.3 ⚠️ 常见陷阱
- 在依赖里创建长连接却不复用 → 连接池耗尽。
- 在 `lifespan` 内抛异常未处理 → 应用启动失败但无清晰日志。
- 忘记关闭 background scheduler / 消息消费者 → 进程无法优雅退出。

---

## 5. 综合示例：从单体到可测试的模块化

### 5.1 单体版本（简化）
```python
# monolith.py
from fastapi import FastAPI, Depends
from sqlalchemy.orm import Session
from db import SessionLocal

app = FastAPI()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

@app.get("/users/{uid}")
def get_user(uid: int, db: Session = Depends(get_db)):
    return {"id": uid}
```
问题：无法局部替换依赖、无法复用跨模块逻辑。

### 5.2 模块化 + 测试友好
```python
# app/deps.py
from functools import lru_cache
from pydantic import BaseSettings
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session

class Settings(BaseSettings):
    db_url: str = "sqlite:///./app.db"

@lru_cache
def get_settings() -> Settings:
    return Settings()

def get_engine(settings: Settings = Depends(get_settings)):
    return create_engine(settings.db_url, connect_args={"check_same_thread": False})

def get_db(engine=Depends(get_engine)):
    SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# app/routers/users.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from app.deps import get_db

router = APIRouter(prefix="/users", tags=["users"])

@router.get("/{uid}")
def get_user(uid: int, db: Session = Depends(get_db)):
    return {"id": uid, "name": "Alice"}

# app/main.py
from fastapi import FastAPI
from app.routers import users

app = FastAPI()
app.include_router(users.router)
```

### 5.3 测试覆盖依赖
```python
# tests/test_users.py
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.main import app
from app.deps import get_db

def override_db():
    engine = create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})
    TestingSession = sessionmaker(bind=engine)
    db = TestingSession()
    try:
        yield db
    finally:
        db.close()

app.dependency_overrides[get_db] = override_db
client = TestClient(app)

def test_get_user():
    r = client.get("/users/1")
    assert r.status_code == 200
```

---

## 6. ⚠️ 常见陷阱

- 在 async 路由中用同步 IO（阻塞）且未放线程池 → QPS 猛跌。
  解决：`run_in_threadpool` 或改用异步驱动。
- 每次请求里 new Engine / SessionLocal → 连接池被频繁创建销毁。
  解决：Engine 放在 `lifespan` 或模块级单例。
- 忘记在 `yield` 后关闭连接 → 文件描述符泄漏。
- 依赖链出现循环引用（A depends B，B depends A）→ 启动时报错，注意解耦。
- 路由拆分后忘记在 `main.py` include → 404 但无报错，务必有自检测试。

---

## 7. 💡 最佳实践

- **小函数、可组合**：依赖应职责单一（拿配置、建会话、取当前用户），再组合成链。
- **显式资源管理**：对 DB/Redis/消息队列使用 `yield`，或在 `lifespan` 中集中管理。
- **router 分层**：领域路由 + 子标签；局部中间件仅作用于需要的 router。
- **测试先行**：先写依赖覆盖的测试，再补业务；确保 `dependency_overrides` 简洁可见。
- **配置可替换**：Settings 用 Pydantic BaseSettings；在测试覆盖配置依赖，模拟不同环境。
- **对 Java 同学**：把 `Depends` 当作"函数式 Bean 注入"，不需要 XML/注解扫描，文件即配置。

---

## 8. 练习（建议放 labs）

1. **lab01_router_splitting**
   - 任务：将单文件接口拆成 `users.py` / `products.py` / `health.py` 三个 router，并在 `main.py` 挂载。
   - 验收：`/health` 无需鉴权，其余路由正常响应。

2. **lab02_dep_chain_override**
   - 任务：实现配置→引擎→Session→仓储→路由的依赖链；写一个内存 SQLite 覆盖的测试，验证 CRUD。
   - 验收：pytest 通过；覆盖后不触碰真实 DB。

3. **lab03_lifespan_resource**
   - 任务：在 `lifespan` 创建 Redis 连接池，路由读取 `app.state.redis`；编写 teardown 关闭连接。
   - 验收：启动与关闭时日志正确；连接池对象在路由可用。

4. **lab04_ab_switch**（可选）
   - 任务：实现 `get_feature_flag()` 依赖，默认返回 True；测试中覆盖为 False，验证路由返回不同字段。
   - 验收：两组测试分别断言响应差异。

---

## 9. Java vs Python 小贴士

- **作用域心智**：Spring 的 singleton/request/session → FastAPI 默认"每请求一实例"，需要缓存时自己加 `lru_cache` 或放 `app.state`。
- **配置注入**：Spring `@Value`/`@ConfigurationProperties` → FastAPI 用 Pydantic BaseSettings + 环境变量。
- **拦截器**：Spring Filter/Interceptor → FastAPI Middleware；同样可以设置局部 router 中间件。
- **切面**：简单场景用中间件或依赖函数；复杂 AOP 可用装饰器组合。
- **MockBean**：对应 FastAPI 的 `dependency_overrides`，无需额外容器魔法。

---

## 10. 小结

- 路由拆分用 APIRouter，按领域/版本/中间件需求分层。
- `Depends` 既做参数校验，也做依赖注入；多级链让可替换性和可测试性大幅提升。
- `dependency_overrides` 是测试与多配置开关的万能钥匙。
- `lifespan` 负责"进程级"资源，`yield` 依赖负责"请求级"资源。
- 保持函数小而纯，测试先行，才能让 FastAPI 项目像拼乐高一样扩展。
